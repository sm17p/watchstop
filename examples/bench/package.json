{
  "name": "@watchstop/example-bench",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "bench:browser": "playwright test",
    "bench:browser:headed": "BENCH_HEADED=1 playwright test --headed"
  },
  "dependencies": {
    "@watchstop/core": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "^5.9.3",
    "vite": "^8.2.0"
  }
}
