---
"@watchstop/angular": minor
---

Add `@watchstop/angular` with `injectStopwatch()`, bridging `Store` into a readonly Angular signal and tearing down via `DestroyRef`. Construction stays inert until `start()`. `@watchstop/core` is a peer dependency.
