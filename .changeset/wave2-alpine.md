---
"@watchstop/alpine": minor
---

Add `@watchstop/alpine` with `createStopwatch()`, a factory binding with Alpine `init` / `destroy` lifecycle hooks for subscribe and teardown. No plugin helper — single entry point. `@watchstop/core` is a peer dependency.
